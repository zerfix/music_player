
trait Input {
    fn up(&mut self);
    fn down(&mut self);
    fn left(&mut self);
    fn right(&mut self);
    fn pg_down(&mut self);
    fn pg_up(&mut self);
    fn start(&mut self);
    fn end(&mut self);
}
